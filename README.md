# MLP-From-Scratch-Java
Multi-Layer Perceptron (MLP) implemented completely from scratch in Java without using any machine learning libraries. Includes forward propagation, backpropagation, gradient descent, model evaluation metrics, and interactive predictions.
